_schema-version: "3.2"
ID: productinventorymanagement
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: productinventorymanagement-destination-content
  type: com.sap.application.content
  requires:
  - name: productinventorymanagement-destination-service
    parameters:
      content-target: true
  - name: productinventorymanagement-repo-host
    parameters:
      service-key:
        name: productinventorymanagement-repo-host-key
  - name: productinventorymanagement-uaa
    parameters:
      service-key:
        name: productinventorymanagement-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productinventorymanagement_html_repo_host
          ServiceInstanceName: productinventorymanagement-html5-service
          ServiceKeyName: productinventorymanagement-repo-host-key
          sap.cloud.service: productinventorymanagement
        - Authentication: OAuth2UserTokenExchange
          Name: productinventorymanagement_uaa
          ServiceInstanceName: productinventorymanagement-xsuaa-service
          ServiceKeyName: productinventorymanagement-uaa-key
          sap.cloud.service: productinventorymanagement
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: productinventorymanagement-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productinventorymanagement-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productinventorymanagement.zip
      name: productinventorymanagement
      target-path: resources/
- name: productinventorymanagement
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productinventorymanagement-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productinventorymanagement-destination-service
    service-plan: lite
- name: productinventorymanagement-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: productinventorymanagement-xsuaa-service
    service-plan: application
- name: productinventorymanagement-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productinventorymanagement-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
